import { Request, Response } from "express";

export async function getTickSightings(req: Request, res: Response) {
    res.send("WIP");
}

export async function getAllTicks(req: Request, res: Response) {
    res.send("WIP");
}